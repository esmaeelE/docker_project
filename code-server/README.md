# Code Server

VS Code in the browser via [linuxserver/code-server](https://github.com/linuxserver/docker-code-server).

## Run

```bash
cp .env.example .env   # set passwords
docker compose up -d
```

App available at `http://localhost:8443`.